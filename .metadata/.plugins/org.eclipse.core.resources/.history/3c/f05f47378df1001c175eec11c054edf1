package test;

import java.util.ArrayList;

import algorithms.Algorithms;
import graph.*;

public class Test {
	public static void main(String[] args) {
		Graph aGraph = new Graph(true);
		aGraph.addVertex(0);
		aGraph.addVertex(1);
		aGraph.addVertex(2);
		aGraph.addVertex(3);
		aGraph.addEdge(0, 1, aGraph.getIsDirected(), 12);
		aGraph.addEdge(0, 2, aGraph.getIsDirected(), 12);
		aGraph.addEdge(0, 3, aGraph.getIsDirected(), 12);
		for(Vertex i : aGraph.getAdjacent(0)) {
			System.out.println(i.getId());
		}
		if(aGraph.getOneVertex(5) != null) 
			System.out.println("flag");
		else
			System.out.println("no");
		ArrayList<Algorithms> a = new ArrayList<Algorithms>();
	}
}
